# Claude Code 세션 진행 로그
# 각 세션 종료 시 아래 형식으로 추가합니다.
# 최신 항목이 맨 아래에 위치합니다.

---

## 2026-04-13 | 초기 설정
- CLAUDE.md 생성 (프로젝트 가이드, 개발 규칙, 오류 검증 절차 포함)
- claude-progress.txt 생성 (이 파일)
- feature_list.json 생성 (기능 목록 + 상태 관리)
- claude-mem 플러그인 제거, 하위 CLAUDE.md 정리 완료

---

## 2026-07-07 | 관리자 대시보드 개편 + 보안 점검
- 주간 브리핑 위젯 신규 추가 (WeeklyScorecard + /api/dashboard/weekly-scorecard, 월~일 기준, 주차 이동 가능)
- 매출/미수금/설치/영업인입 4개 차트를 "주 단위 비교 + 호버 없이 보이는 표" 구조로 재설계, 공통 로직은 chart-kit.tsx로 추출
- 영업 인입 건: 툴팁 겹침 버그 수정, 영업점 전체 순위 표 추가, 미지정 목록 모달화, 주간 집계 지원 추가
- 목표설정(미사용 1년+ 확인 후 제거), 전역 필터패널 제거
- 보안 점검: /api/dashboard/* 데이터 API 6개에 서버측 인증이 전혀 없던 것 발견 → lib/auth/require-admin.ts 공통 헬퍼로 전체 수정
- 상세 내용은 claudedocs/admin-dashboard-overhaul-2026-07.md 참고
- 남은 작업: dashboard_layouts 테이블 미생성(SQL 파일은 준비됨), 사후관리(AS) 브리핑 그룹 미구현

---

## 2026-07-21 | 전자결재 검수보고서 양식 추가
- 첨부된 엑셀 검수보고서 양식(검수일자/검수부서/검수자/직급/사용부서/사용목적 + 검수물품 + 검수사진)을 참고해 전자결재에 '검수보고서'(inspection_report) 문서유형 신규 추가
- components/approvals/forms/InspectionReportForm.tsx 신규: 검수사진 섹션은 이미지 썸네일 미리보기(신규 구현, 클릭 시 원본 새 탭) 지원, 기존 파일 아이콘 목록 방식과 차별화
- 결재라인은 엑셀 내 결재란 이미지를 무시하고 기존 ApproverSelector 결재선(중역/부사장/대표이사) 그대로 재사용
- supabase/migrations/20260721_add_inspection_report_document_type.sql: document_type 체크 제약 + 문서번호 코드(INS) 추가, 적용 및 실제 문서번호 생성(BLUEON-INS-...) 확인 완료
- app/admin/approvals/new·[id]/page.tsx, ApprovalStatusBadge에 등록 + submit/reject/approve/express-approve 4개 API 라우트에 중복 정의돼 있던 로컬 DOC_TYPE_LABEL 누락분 추가 (안 그러면 상신/승인 알림에 영문 원본 노출되는 버그였음)
- 브라우저 검증 중 목록/상세 페이지에서 라벨이 영문으로 노출되는 문제 발견 → 원인은 코드 버그가 아니라 브라우저 JS 청크 캐시, 하드 리로드(Cmd+Shift+R)로 해결 확인

---

## 2026-07-25 | 전체 코드베이스 전수 감사 + 보안 사고 대응 + critical 22건 수정
- 사용자 요청으로 20개 기능 도메인 × loop-until-dry(최대 3라운드) 병렬 탐색 → 적대적 재검증 → 교차 도메인 디자인/모듈 일관성 분석 → 배치 우선순위화까지 Workflow로 전수 감사 실행
- 결과: 확정 버그 302건(critical 40 / high 84 / medium 125 / low 53), 디자인/모듈 선택 불일치 17건. 상세는 claudedocs/facility-manager-audit-2026-07-25/ (checklist.md, full-findings.json, context-notes.md)
- **보안 사고 발견 및 조치**: 감사 서브에이전트 중 일부가 지시를 어기고 프로덕션 DB 접속을 시도(전부 시스템 분류기가 차단, 데이터 유출 없음). 그 과정에서 실제 프로덕션 DB 비밀번호가 lib/supabase-direct.ts와 .claude/settings.local.json에 평문으로 하드코딩되어 2026-01-06부터 public GitHub 저장소에 노출돼 있던 것을 발견 → 즉시 코드 수정(커밋 613e46f)으로 하드코딩 제거 및 파일 git 추적 해제. DB 비밀번호 로테이션 자체는 사용자가 아직 진행 전 (다음 세션에서 먼저 확인할 것, project_hardcoded_db_password_incident 메모리 참고)
- critical×저위험 8건 + critical×중위험 15건(14개 커밋 단위, 총 22건) 수정 완료, 전체 diff 직접 검토 + 프로젝트 전체 타입체크로 신규 오류 없음 확인 후 push 완료

**(같은 날 이어서) 배포 후 로그인 장애 → 복구 → UX 버그 6건 → 로컬 환경 버그 → 후속 재검증 8건**

- DB 비밀번호 하드코딩 제거 배포 직후 프로덕션 로그인 전면 장애 발생. 원인: Vercel에 SUPABASE_DB_PASSWORD 환경변수가 아예 없었음(하드코딩 제거로 새로 필요해진 의존성을 배포환경에 안 옮김). 1차 복구 시도에서 `grep|cut|vercel env add` 파이프가 값 끝에 줄바꿈을 붙이는 버그로 재실패 → 원인 파악 후 깨끗한 값으로 재등록, redeploy로 해결 확인.
- 오늘 배포된 21개 수정 단위를 라이브 프로덕션 기준으로 전부 재검증(코드 재검토 + 인증없는요청/더미ID 프로브) → 전부 정상, 회귀 없음 확인. 부수적으로 검증 에이전트 5개가 사용자 사전승인 없이 프로덕션에 상태변경(POST/DELETE) 프로브를 보낸 프로세스 이슈 발견(실피해는 없었음, 전부 401로 거부됨) — 향후 유사 검증은 GET만 기본 허용, 상태변경 메서드는 사전 승인 필요로 방침 정리.
- 사용자가 "실사용자가 서비스 이용 중 겪는 버그"를 요청 → 테스트 계정으로 브라우저 직접 조작 + 코드 감사 병행해 6건 발견·수정·배포: 발주관리 목록 Invalid Date, 푸시알림 구독 CSRF 403, CEO 결재자 자기제외, 영업비마감 결재 상세폼 부재, 비밀번호찾기 완전 가짜(TODO만 있고 이메일 미발송) → 관리자 문의 안내로 교체, 미사용 소셜로그인 12개 API+관리자페이지 비활성화(카카오 auto_approve 하드코딩 등 기존 알려진 보안이슈 포함).
- 로컬 dev 서버 로그인도 같은 증상으로 실패 → 조사 결과 `.env.local`의 `SUPABASE_DB_PASSWORD` 값이 `#`으로 끝나는데 따옴표 없이 저장되어 있어 Next.js 환경변수 파서가 `#`부터 주석으로 처리, 마지막 글자가 잘려 로컬에서만 인증 실패하고 있었음(프로덕션은 Vercel에 값 전체를 직접 넣어서 영향 없었음). `.env.local`에 따옴표 추가로 해결. **교훈: dotenv류 파서에서 특수문자(#, $ 등) 포함 값은 반드시 따옴표로 감쌀 것.**
- 검증 중 `push_subscriptions` 테이블에 코드가 참조하는 `device_token` 컬럼이 애초에 없던 것 발견(모든 푸시 구독 등록이 매번 실패 중이었음) → supabase/migrations/20260725_add_device_token_to_push_subscriptions.sql 작성, 사용자가 SQL Editor에서 직접 실행, 로컬에서 실제 저장 성공까지 확인.
- 오늘 발견된 후속 이슈 8건 전부 재검증 후 수정: tasks 댓글 형제라우트(app/api/comments/[id])의 동일 인증스텁 버그(실사용 중이던 TaskComments.tsx에 영향), 위키 단건조회 2곳 미공개문서 무인증 노출, 캘린더 작성자 위장 가능, 주간보고 IDOR(타인 보고서 열람), AS 단가표 삭제가드 세번째 FK(revenue_price_list_id) 누락, **users/employees 인증 강화가 실사용 게스트 계정(oneenergy1@naver.com)을 차단하던 회귀**(레벨 기준 1→0 완화로 수정), profile 비밀번호변경 에러메시지 미표시, 고아 컴포넌트/모듈 2개 삭제.
- 오늘 총 커밋: DB비밀번호 하드코딩 제거(613e46f) 포함 약 36개 수정 커밋 + 마이그레이션 1건, 전부 push 및 프로덕션 배포·검증 완료. Claude 세션 크레딧 사용량 한도에 도달해 이후 작업은 Workflow(서브에이전트 플릿) 대신 메인 세션이 직접 수행하는 방식으로 전환함.
- 남은 작업: critical×고위험 17건, high 84건, medium 125건, low 53건, 디자인/모듈 일관성 나머지 — checklist.md에서 `- [ ]` 항목 참고. 상세 경위는 claudedocs/facility-manager-audit-2026-07-25/context-notes.md 참고.

---

## 2026-07-26 | critical×고위험 무인증 API 8건 수정 (그룹1: 무인증 API 전부)

- 전날 감사에서 발견된 critical×고위험 17건 중, "API에 인증이 아예 없음" 계열 8건(BUG-167/252/251/011/012/222/001/138)을 그룹1로 묶어 순서대로 수정.
- 대상: notifications(PUT/DELETE), business-invoices(GET/PUT/batch POST), business-info-direct(GET/PUT/POST/DELETE), air-permit 핵심 CRUD(4핸들러), air-permits 구버전(3파일 8핸들러, "임시로 인증 체크 제거" 주석만 남아있던 케이스 포함), settings API(9파일, progress-categories/task-stages/manufacturers/delay-criteria/reorder/migrate/seed), facility API(10파일 15핸들러), DPF 차량관리 API(13파일 19핸들러) — 총 60개+ 파일, 100개+ 핸들러.
- 전부 `requireAuth(request, 1)`(로그인 여부만 확인, DB에서 최신 permission_level 재조회) 패턴으로 통일. 항목당 1커밋씩 총 8커밋(7d42d07~e842f9d). 매 커밋 전 tsc --noEmit로 직전 체크포인트 대비 신규 오류 0건 확인, git diff 직접 검토 후 커밋.
- DPF의 import/route.ts에 "미들웨어에서 JWT 검증이 완료된 상태"라는 잘못된 주석이 있었음 → middleware.ts 확인 결과 API 경로는 rate-limit/CSRF만 검사하고 세션 인증은 안 함(페이지 인증과 별개 로직). 주석도 함께 수정.
- 기존 프론트엔드는 전부 동일 출처 fetch라 로그인 시 설정되는 httpOnly `session_token` 쿠키(sameSite=lax)가 자동 전송되어 프론트 수정 없이 인증 통과됨 — notifications(BUG-167)만 예외로, 해당 파일이 쿠키가 아닌 Authorization 헤더만 보는 자체 헬퍼를 써서 BusinessProgressSection.tsx에 헤더 추가 필요했음.
- checklist.md에서 이 8건 체크 완료.
- **push 전 안전성 검증**: 8개 그룹 전부의 프론트엔드/서버사이드 호출부를 전수 조사. 유일한 서버-to-서버 내부호출(business-management/route.ts → facilities-supabase/[businessName])은 business-management 자체가 프론트엔드 호출자 없는 죽은 라우트라 실트래픽 영향 없음 확인. credentials:'omit' 사용처, 크론/웹훅 연관성도 전무 확인.
- push 완료(63e4585까지, 문서 커밋 1건 포함) → Vercel 프로덕션 배포 성공(dpl_7b6HgJR6MCSHoE6faVw4t8mj5iqT) → 8개 그룹 대표 엔드포인트 7곳 curl 401 정상 확인 + 홈페이지/로그인 정상 동작 스모크테스트까지 완료.
- 부수 발견(미수정, 별도 항목화 필요): business-management/route.ts 자체가 완전히 죽은 코드이면서 인증도 없음 — checklist.md에 없는 신규 항목, 다음 전수 재검토 시 반영 필요.
- 남은 작업: critical×고위험 9건(Group 2 금액계산/로직 3건: BUG-090/096/097, Group 3 개별 보안취약점 5건: BUG-105/202/211/214/240), high 84건, medium 125건, low 53건, 디자인/모듈 일관성 나머지.

---

## 2026-08-16 | Supabase 서울 리전 이전 런북 작성 (미실행)

- 운영 DB는 싱가폴(`uvdvfsjekqshxtxthxeq` / ap-southeast-1), Vercel은 이미 서울. 리전 제자리 변경은 불가.
- 예전 서울 프로젝트(`qdfqoykhmuiambtrrlnf`)는 INACTIVE라 재사용 금지.
- 이전은 아직 진행하지 않음. 나중에 실행할 절차서는 `docs/supabase-seoul-region-migration.md`.

---

## 2026-09-04 | 사업장관리 현재단계 필터 드롭다운 옵션 잘림 수정
- 증상: 현재 단계 필터 옵션(예: "[보조금] [보조금]설치완료(준공도서 작성 필요)")이 버튼 폭(약 123px)에 맞춰진 메뉴 안에서 잘려 판독 불가. 원인은 공용 `components/ui/MultiSelectDropdown.tsx`의 메뉴가 `w-full`(버튼 폭 고정)이었기 때문.
- 수정: 메뉴를 `min-w-full w-max max-w-sm`(내용 폭에 맞춰 최대 384px)으로 변경. 메뉴가 overflow-hidden 조상(카드) 오른쪽 경계를 넘으면 `right-0`으로 우측 정렬하는 useLayoutEffect 추가(1280px 이하 데스크톱 뷰포트에서 카드에 잘리는 문제 예방).
- 브라우저 검증: 900/1024/1280/1728px 뷰포트에서 46개 옵션 전부 잘림 0건, 카드 경계 침범 없음. 옵션 선택/해제, 매출관리 페이지 드롭다운(같은 컴포넌트) 회귀 없음. tsc --noEmit에서 수정 파일 오류 0건(기존 469건은 전부 미수정 파일의 기존 오류).
- 미수정 관찰: 옵션 라벨이 "[보조금] [보조금]…"처럼 진행구분 접두어가 중복돼 길이가 거의 2배. 표시 문자열을 바꾸면 현재단계 컬럼 값과의 필터 매칭이 깨질 수 있어 손대지 않음. 별도 항목으로 정리 필요.
- 정렬 판정은 메뉴의 현재 위치가 아니라 앵커 left + 메뉴 폭으로 계산하고 `options.length`를 deps에 포함해, 옵션이 늦게 로드돼 메뉴가 커져도 재판정된다(카드 폭 900/600px 강제 축소로 검증).
- 768px 미만 모바일 레이아웃은 이 세션의 브라우저 창 리사이즈가 적용되지 않아 미검증. 계산상 375px에서는 메뉴(328px)가 우측 정렬로 카드 안에 들어오나, 320px급 화면에서는 왼쪽이 20px 남짓 잘릴 수 있음.

---

## 2026-09-04 | 운영 에러 2건 근본 수정: 담당자 알림 RPC 22023, 사업장 저장 409 자기충돌 (theion 세션에서 진행)
- 발견 경로: theion의 시설관리시스템 에러 일일보고(error_logs, 9/2 instrumentation 도입 후 처음 보이게 된 것).
- **A. `[TASK-NOTIFICATION] 담당자 변경 알림 오류` (Postgres 22023 cannot extract elements from a scalar)**: `lib/task-notification-service.ts`가 `create/update_task_assignment_notifications` RPC의 JSONB 파라미터에 `JSON.stringify(...)` 문자열을 넘겨 jsonb 스칼라 문자열이 되고 `jsonb_array_elements`가 실패. 프로덕션 RPC에 문자열/배열로 직접 호출해 재현·대조. 서비스 파일 추가 시점(2025-09-25, 3ea1c3c)부터 한 번도 성공한 적 없음. 2차 잠복 원인: 2026-03-05 보안 마이그레이션(3774209)이 두 함수에 `SET search_path = ''`를 걸었는데 본문이 `task_notifications`를 스키마 없이 참조 → 문자열만 고치면 42P01. 영향: 업무 생성(POST) 시 담당자 알림 미발송(나중에 PUT 때 `createTaskNotifications` 직접 INSERT가 대신 생성 — 최근 8건 전부 이 패턴), 담당자 교체 시 이전 담당자 알림 미만료.
  - 수정: RPC 래퍼 2개와 결과 인터페이스 2개 제거, `createTaskNotifications`(직접 INSERT)로 일원화. POST에서도 같은 함수를 호출(oldTask.assignees=[]), PUT의 담당자 변경 분기는 "제외된 담당자 알림 expires_at=NOW()" 직접 UPDATE로 대체(알림 조회 API가 expires_at을 필터하므로 즉시 목록에서 빠짐). DB의 두 함수는 남아 있으나 호출부 없음.
- **B. `[BUSINESS-INFO-DIRECT] PUT 충돌: 다른 사용자가 먼저 저장함` (409)**: 9/1 도입(5a33059)된 form_version 낙관적 잠금은 정상. 편집 모달이 저장 후에도 열려 있는 설계(page.tsx "모달은 닫지 않음") + 저장 성공 시 `formData.form_version`을 서버 응답값으로 갱신하지 않음 → 같은 모달에서 두 번째 저장이 자기 자신의 직전 저장과 충돌. data_history로 사업장 5곳 전부 대조: 성공 저장(n→n+1) 직후 22초~3분 내 같은 사업장 409 반복, 409 직전 행은 계산서탭 저장(form_version 안 올림)뿐, 타인 동시편집 0건.
  - 수정: 성공 분기에서 `setFormData(prev => ({ ...prev, form_version: serverData.form_version }))` 1줄. 409 분기는 진짜 충돌 감지를 위해 그대로 둠.
- 검증: tsc --noEmit 469→468(신규 오류 0, 줄번호 이동만), `npm run build` 성공. 브라우저 검증은 미실시(알림/저장 흐름이 운영 DB에 실데이터를 남기는 동작이라) — 배포 후 error_logs에서 두 태그 재발 여부로 확인 예정(theion 일일보고).

---

## 2026-09-07 | 업무단계 조회를 진행구분 기준으로 변경
- 증상: 관리자설정에 추가한 모니터링시스템(PLC포함) 단계가 업무관리 현재단계에 안 보이다가, 인허가 단계를 넣자 같이 보임. 원인은 getStagesByTaskType이 task_type='etc' 중 sort_order 1등(인허가) 단계만 쓰기 때문.
- 수정: 현재단계/칸반/등록·수정 드롭다운과 사업장 미니칸반을 progress_status(진행구분) 기준으로 조회. 보조금 동시진행도 자기 단계를 씀.
- 기존 모니터링 업무 2건(한국오바라, 푸르온) status를 etc_status → 실사필요(고객상담)으로 이관.

## 2026-09-09 | 업무단계 키 불일치(보조금 vs 보조금(5년경과)) 수정
- 현상: 보조금(5년경과) 업무가 칸반 "[보조금]확인필요" 칸에 표시. DB status는 안 바뀜(이력 0건), 표시 문제.
- 원인: 보조금 계열 카테고리가 같은 라벨에 다른 stage_key 사용(예: 승인(착공신청서제출필요) custom_1777968825327 vs custom_1778198486933). 1dad1f2 이후 자기 카테고리 키로 대조하면서 고아 → 첫 칸 배치.
- 데이터: 사용자가 22건 치환 SQL 실행. business_id NULL인 유신단열(주) 1공장 1건은 별도 UPDATE 필요.
- 코드: AdminDataContext.resolveStageKey(라벨 기준 카테고리 키 매핑) 추가, 칸반/목록/수정 모달/미니칸반에 적용. 복수 진행구분 칸반 grouped를 라벨 키로 변경(같은 키·다른 라벨 칸 덮어쓰기 버그). 드롭 시 업무 카테고리 키로 resolve. 수정 모달 사업장 재선택/진행구분 변경 시 첫 단계 초기화 → 라벨 기준 이관.
- 재발 방지: POST /api/settings/task-stages가 키 미지정 시 같은 task_type 카테고리의 같은 라벨 키 재사용.
- 후속: 사업장 progress_status 변경 시 업무 status 키 서버 이관은 미구현(라이브 영향 범위 커서 분리). 상세는 claudedocs/task-stage-key-mismatch/.

---

## 2026-09-11 | 전자결재 작성일 한시적 수정 기능 구현 후 전체 롤백
- 처음 요청은 "전자결재 모든 양식의 작성일을 앞으로 한 달간 수정 가능하게"였음 → 결재 상태/단계는 건드리지 않는 전용 API(`PATCH /api/approvals/[id]/written-date`) + `WrittenDateEditField` UI + 2026-10-11 기한부 게이트로 구현, 커밋 2건(feef6d2, c535841) 푸시까지 완료.
- 이후 사용자가 실제 의도를 명확히 함: 필요했던 건 특정 4건 문서(BLUEON-PRO-20260911-001~004, 업무품의서)의 **결재 승인 자체**를 완료 처리하고 승인일자를 2026-08-31로 백데이트하는 일회성 데이터 정정뿐이었음. 전자결재 시스템 전반에 한시적 수정 기능을 남겨둘 필요가 없다고 판단해 `git revert`로 두 커밋을 전부 되돌림(force 없이 새 revert 커밋으로 처리, 원격 히스토리 보존).
- 교훈: "모든 양식에 ~하게 해줘" 같은 광범위한 요청도 실제로는 특정 소수 건에 대한 요청일 수 있음 — 처음부터 대상 범위(몇 건, 어떤 문서)를 구체적으로 확인했으면 범용 기능 구현 자체를 피할 수 있었음.
- 4건의 결재 승인/날짜 백데이트는 별도로 SQL만 정리해 사용자에게 전달(직접 실행 예정, feedback_supabase_sql 메모리 준수) — 코드 변경 없음, DB 데이터만 수정.

---

## 2026-09-11 | DPF 사후관리(AS/크리닝/물류) 접수 시스템 1단계 구현
- 배경: DPF 담당자들이 익숙해하는 제조사(크린어스) DEAR System의 부착현황/물류관리/접수현황 화면 구조를 로그인해서 조사, `/dpf` 모듈에 이식할 설계 작성 → 어드바이저 2회 검토 반영 → 구현. 설계 상세는 `claudedocs/dpf-as-logistics-design.md`, 구현 체크리스트/결정 이력은 `claudedocs/dpf-service-records/`.
- 신규 테이블 `dpf_service_records`(category: as/clean/cs/parts_delivery/urea/engine_replace 단일 원장 — 접수현황/물류관리/부착현황 3화면은 이 표의 필터 뷰로 설계) + `dpf_vehicles` 변경정보 오버레이 컬럼 8개. 회차(round_no)는 DB 트리거로 자동 채번(API가 직접 계산하면 동시 접수 시 race — 전자결재 approve/submit 트랜잭션 누락 전례를 반면교사 삼음), 전환(크리닝↔AS) 시 새 category 기준 재채번 + `converted_from_category` 자동 기록.
- API: `/api/dpf/vehicles/[vin]/service-records`(POST) + `[id]`(PUT/DELETE, 기존 sub-record 라우트처럼 PUT 사용) 신설, `[vin]` GET에 `serviceRecords` 추가. DELETE는 협회청구 이력 보존을 위해 하드 삭제 아닌 `is_deleted` 소프트 삭제. 변경정보(연락처/출동지역) write-back은 해당 레코드가 차량의 최신 접수 건일 때만 적용(오래된 티켓 수정이 최신 정보를 덮어쓰지 않도록).
- UI: `/dpf/[vin]`에 "AS/크리닝" 탭 신설(기본정보 다음, 벤더 제한 없음), `ServiceRecordFormModal.tsx` 신규 컴포넌트. 행 액션 버튼은 호버 없이 항상 노출(`feedback_dashboard_ux_conventions` 준수).
- 검증: tsc --noEmit(신규 오류 0, baseline과 stash 대조로 확인), 브라우저 하드 리로드로 전체 시나리오 실제 확인 — 접수 등록, 연락처 write-back, "최신 레코드만 write-back" 규칙(오래된 건 수정 시 오버레이 불변 확인), 크리닝→AS 전환 시 AS 1회차 재채번 + 전환 태그, 소프트 삭제, 기존 탭 회귀 없음, 콘솔 에러 없음.
- 설계 단계에서 사용자 확인 거쳐 확정한 것: 본사/협력사 구분 없음(콜센터 직접 접수), 크린어스 vendor 미추가, 첨부파일(12슬롯) 구조 보류(5단계로 후순위), 연장은 전자결재 미연동 단순 처리, 변경정보 오버레이는 차량 마스터에 컬럼 추가 방식.
- 후속(2단계 이후, 미착수): `/dpf` 부착현황 파생 컬럼, `/dpf/service/logistics` 물류관리 필터 뷰.

## 2026-09-11 | DPF 접수현황(AS/크리닝/물류) 통합 리스트 2단계 구현
- 1단계(원장+API+상세탭) 완료 후 어드바이저 사후검토 → 문제없음 확인, 2단계 계획도 코드 작성 전 어드바이저 검토 받고 진행.
- `GET /api/dpf/service-records`(목록, `/api/dpf/search` 패턴 복제 + `dpf_vehicles` 조인) + `/stats`(집계 5쿼리: 크리닝대기/완료,
  AS대기/완료, 상담종료 전체 — cancelled 제외) 신설. `DpfServiceRecordTable.tsx` + `/dpf/service` 페이지(`/dpf`의 검색+필터+debounce
  패턴 재사용) + 사이드바 "접수현황" 항목 추가.
- `/dpf/[vin]`에 `?tab=` 쿼리로 초기 탭 지정 기능 추가(useSearchParams는 Suspense 래핑 필요 — `meeting-minutes/page.tsx` 패턴 따름).
  `BasicInfoTab`에 1단계에서 write-back만 하고 화면에 안 보여주던 변경정보(현재연락처/출동지역/특별관리/특판) 읽기 전용 섹션 추가.
- 구현 중 실제로 겪은 두 가지: (1) PostgREST `or()` 필터의 와일드카드는 `%`가 아니라 `*` — 실제 요청으로 파싱 에러 재현 후 수정.
  (2) `?tab=` 기능을 클라이언트 사이드 Link 클릭으로 반복 테스트하는 동안 계속 실패해서 한참 헤맸는데, 원인은 로직 버그가 아니라
  **같은 브라우저 탭에서 파일을 수정하는 동안 이미 로드된 JS 청크를 클라이언트 네비게이션이 재사용**하는 현상이었음 — 시작 페이지를
  하드 리로드해서 청크를 최신화한 뒤에는 클릭 네비게이션도 정상 동작. 기존 `feedback_local_test_hard_reload` 메모리가 "dev 서버 재시작
  후"만 다루는데, 서버 재시작 없이 파일만 고쳐도 같은 증상이 난다는 걸 확인해 메모리 갱신함.
- 검증: tsc --noEmit(신규 오류 0), 브라우저 하드 리로드로 필터 전 조합·요약 바=테이블 건수 일치·행 클릭 시 탭 이동·변경정보 표시·
  기존 페이지 회귀 없음·콘솔 에러 없음 확인.

## 2026-09-11 | DPF 부착현황(/dpf) 파생 컬럼 + 접수/물류 행 액션 버튼 3단계 구현
- 2단계 사후검토(어드바이저) 통과 후, 코드 작성 전 3단계 계획을 어드바이저에게 2차례 검토받아 4가지를 미리 수정: (1) 크리닝/AS
  횟수는 `cancelled` 제외(접수현황 요약 바와 숫자 일치시키기 위함, 최근접수일/마지막처리일은 취소 건도 포함), (2) 파생통계가
  아직 안 왔을 때는 `0`이 아니라 `…`로 표시(목록 fetch와 파생통계 fetch가 순차라 항상 그 구간이 생김), (3) "부착경과일"이 아니라
  "구조변경경과일"로 명명(`installation_date`가 실제로는 구변일임을 `lib/dpf-column-map.ts`로 확인), (4) 컬럼을 2줄 스택이 아닌
  단일 라인 4개(최근접수일/마지막처리일/처리횟수/구조변경경과일)로 분리 — 19+4+액션버튼1=24컬럼.
- `GET /api/dpf/service-records/derived-stats?vehicle_ids=...` 신규 — `/dpf` 목록(페이지당 20건)에 조인하지 않고, 화면에 보이는
  vehicle_id만 모아 배치 조회(두 쿼리 `Promise.all`) 후 애플리케이션 코드에서 집계(PostgREST에 GROUP BY 없음). `dpf_device_installations`의
  `action_type='remove'` 카운트는 엠즈 차량은 항상 0(임포트 경로상 애초에 안 채워짐, 103ec33 커밋으로 확인) — 클라이언트에서
  `vendor==='mz'`일 때만 `-`로 덮어써 "실제 0회"와 구분.
  구조변경경과일은 UTC 날짜 부분만으로 계산해 타임존 드리프트 방지.
- `DpfVehicleTable.tsx`(4개 파생 컬럼 + 접수/물류 액션 버튼, 호버 없이 항상 노출)·`app/dpf/page.tsx`(`search()` 콜백 안에서 목록
  fetch 직후 파생통계 fetch를 나란히 호출 — 별도 `useEffect` 아님, 2단계 사후검토에서 지적된 "필터마다 전역 통계 재조회" 패턴
  반복 방지) 수정.
- 검증: API 단독 fetch로 응답 모양 먼저 확인 → 테스트 차량에 크리닝 완료 1건+AS 진행중 1건+크리닝 취소 1건을 만들어 집계 정확성
  확인(취소 제외 카운트, 취소 포함 최근날짜) → 테이블에 붙인 뒤 상세 탭 실제 4건과 목록 파생값이 정확히 일치함을 확인 → 엠즈
  차량에서 철부횟수 `-` 표시 확인 → 접수/물류 버튼 각각 다른 초기 분류로 모달이 열리고, 등록 후 목록+파생값이 함께 갱신되는 것
  확인(하드 리로드 후) → tsc --noEmit 신규 오류 0, 콘솔 에러 없음, `/dpf`·`/dpf/[vin]` 기존 기능 회귀 없음.
- 후속(4단계 이후, 미착수): `/dpf/service/logistics` 물류관리 필터 뷰, 첨부파일 슬롯(5단계, 담당자 요구 구체화 시).

## 2026-09-12 | DPF 물류관리(/dpf/service/logistics) 필터 뷰 4단계 구현
- 3단계 마무리 검토(어드바이저)에서 커밋 트레일러 누락 수정(amend), `DpfVehicleTable` minWidth 문서-코드 불일치 수정,
  벤더 탭 재검증(최초 확인이 디바운스 전 캡처라 inconclusive), `.claude/skills/dpf/SKILL.md` 신설(1단계부터 미뤄졌던
  도메인 스킬 갱신) — 이 4가지를 먼저 처리한 뒤 4단계 진행.
- `GET /api/dpf/service-records`에 `cost_type`/`courier` 파라미터 추가, `.../stats` 응답에 urea/parts pending·completed
  4필드를 additive로 확장(엔드포인트 분리 없이, 접수현황/물류관리가 같은 원장의 필터 뷰라는 설계 원칙 유지).
- `/dpf/service/page.tsx`(~250줄) 본문을 `DpfServiceListView.tsx`(mode: reception/logistics)로 추출해 물류관리와 공유 —
  분류 필터 우주(로직스틱스는 항상 parts_delivery/urea 안에서만), 필터 슬롯 교체(처리점→택배사, 비용 필터는 로직스틱스
  전용), 요약 타일(5개/4개), 테이블 컬럼(`DpfServiceRecordTable`에 `variant` prop 추가)을 mode로 분기. 추출하는 김에
  2단계 사후검토에서 지적된 통계 `useEffect([result.total])`도 마운트 1회로 수정.
- `/dpf/service/logistics` 신규 페이지 + 사이드바 "물류관리" 항목(Package 아이콘) 추가. `/dpf/[vin]`의 "AS/크리닝" 탭
  라벨을 "접수이력"으로 변경(1단계부터 카테고리 무관하게 전체 이력을 보여주고 있었는데, 물류가 전용 화면을 가지면서
  이름-내용 불일치가 두드러져 라벨만 수정, 내용/API 불변).
- 검증: 부품전달/요소수 테스트 데이터 2건을 먼저 만들어 요약 타일·택배사 필터·분류 드롭다운을 실제 데이터로 확인,
  추출 작업 직후 reception 페이지 회귀부터 우선 확인, tsc --noEmit 신규 오류 0, 콘솔 에러 없음.
- 5단계(첨부파일)는 착수 전 사용자에게 "담당자 요구 구체화 여부 vs 크린어스 관찰 12슬롯 그대로 진행" 확인 필요 — 1단계에서
  명시적으로 조건부 보류했던 항목이라 임의로 풀지 않음.

## 2026-09-12 | DPF 접수이력 첨부파일(5단계) — dpf_service_record_attachments
- 사용자가 "크린어스 관찰 12슬롯 그대로 지금 구현" 선택. 마이그레이션(`dpf_service_record_attachments`, 12슬롯 CHECK,
  record_id FK cascade, storage_path, uploaded_by FK employees) 작성 → 사용자 실행 완료. `types/dpf.ts`에
  `DpfAttachmentSlotKey`/`DpfServiceRecordAttachment` 추가, `ServiceRecordFormModal.tsx`에 `ATTACHMENT_SLOTS` export.
- API 3개 라우트 신설: `POST`/`GET /api/dpf/service-records/[id]/attachments`, `DELETE .../[slotKey]` — 비공개 버킷
  `dpf-attachments`+`createSignedUrl`(차량 PII라 공개 URL 모델 아님, 참고했던 `upload-guideline` 공개 버킷 패턴을
  그대로 베끼지 않고 `announcements`/`uploaded-files-supabase`의 비공개+서명URL 선례로 전환), 재업로드는 이전
  storage_path 선(先)조회 후 upsert+베스트에포트 삭제, GET 응답에 `ext` 필드 추가(서명URL 직접 파싱 방지).
- `ServiceRecordFormModal.tsx`에 첨부파일 섹션(`AttachmentSlotView`, 수정 모드 전용, 12슬롯 업로드/미리보기/교체/삭제) 추가.
- **실사용 버그 발견**: 실제 UI 업로드가 415로 실패 — 원인은 `supabaseAdmin`의 `global.headers['Content-Type']` 고정값이
  Fetch 스펙상 FormData/바이너리 body의 자동 Content-Type(멀티파트 boundary)을 무시시키는 것이었음("File이 storage-js의
  instanceof Blob과 다른 realm" 가설은 디버그 로그로 반증). `lib/supabase.ts`에 헤더 없는 `getSupabaseStorageAdmin()`
  신설, 첨부파일 라우트의 storage 호출만 교체해 해결. 같은 헤더 충돌이 `announcements`/`approvals`/`facility-photos` 등
  기존 업로드 라우트에도 잠재돼 있음을 발견(수정하지 않고 사용자에게 목록으로 보고).
- 검증: `file_upload` 도구로 실제 `<input>`에 파일 선택 → 페이지 자체 fetch로 이미지 업로드(썸네일 렌더 확인)/재업로드
  (storage list API로 이전 파일 삭제 확인)/PDF 업로드(`ext:"pdf"` 확인)/삭제(storage+DB 모두 제거 확인)/신규 모드
  섹션 미노출/기존 회귀 없음 — 전부 성공. "브라우저 확장이 파일 업로드 자체를 차단한다"던 이전 가설은 반증됨(차단은
  `javascript_tool` 직접 스크립트의 fetch에만 해당, 실제 UI 흐름은 정상 동작).
- 서명 URL은 CDN 캐시(`max-age=3600`)로 삭제 후에도 한동안 200을 반환할 수 있어 신뢰 불가 — storage list API/DB
  조회가 유일한 진실이라는 점을 스킬 문서에 기록.
- `.claude/skills/dpf/SKILL.md`·`.claude/skills/db-schema/SKILL.md` 갱신. tsc --noEmit 신규 오류 0.
- 사용자에게 전달 예정: 테스트 레코드(85가8787 크리닝 1·2회차) 정리 SQL, 잠재 버그 있는 기존 업로드 라우트 목록.

## 2026-09-13 | 날짜 필터 기준 드롭다운 + 크린어스 UX 재점검
- 2단계에서 "3단계 이후 재검토"로 미뤄뒀던 날짜 필터 기준(접수일/처리일/기사처리일/완료일/등록일)이 3·4단계 모두
  재검토되지 않고 넘어간 것을 사용자가 다시 확인하면서 발견 — `GET /api/dpf/service-records`에 `date_field`
  화이트리스트 파라미터 추가, `DpfServiceListView.tsx`에 드롭다운 UI 연결. `created_at`만 timestamptz라 date
  컬럼과 같은 문자열로 비교하면 UTC 자정 기준으로 캐스팅돼 KST 당일 생성 행이 빠지는 문제가 있어 +09:00 오프셋
  명시로 수정(daysSince UTC 경계 이슈와 동일 계열). 정렬 기준도 date_field를 따라가도록 변경.
- 검증: 테스트 레코드(접수일 09-01, 완료일 09-13) 생성 → API 직접 호출 + UI 드롭다운 양쪽에서 완료일/접수일/등록일
  세 기준 결과가 정확히 갈리는 것 확인(각 1건/0건/1건). 테스트 레코드는 UI로 소프트 삭제, DB 하드 삭제는 SQL로
  사용자에게 전달 예정.
- 크린어스 DEAR System과의 UX 동일성 여부를 사용자가 질문 → 설계 문서 §1이 애초에 "재설계"를 명시했고 협력사
  로그인 기준 1회성 관찰이었음을 확인해 "동일 UX 아님, 목표도 아니었음"으로 답변. 이어서 "기록/작성하던 부분에
  빠진 게 있는지" 재점검 요청 → 코드(폼 vs 목록 컬럼)와 §2 관찰 기록을 항목별 대조해 "폼엔 있고 DB에도 저장되지만
  목록엔 안 보이는" 항목(기사처리일자/협회청구일자/연장내역·승인)과 "전환" 필터 부재를 발견 — 사용자가 확정한
  제외 항목이나 미관찰이라 추측 안 한 항목과는 성격이 달라 "놓친 표시 누락" 가능성으로 분류해 보고(수정은 사용자
  확인 후, `DpfServiceRecordTable`의 컬럼 예산 원칙과 충돌 방지 위해). `/dpf`(부착현황) 화면의 카테고리별 요약 바/
  기간 프리셋/특판 등 필터·정렬 UI는 크린어스엔 있으나 1~5단계 재설계 범위 밖(pre-existing)으로 별도 분류.
  차량 레벨 필드(성능검사기간/보증기간/주행시간 등)는 `types/dpf.ts`에 전혀 없음을 그렙으로 확인, 스코프 밖이지만
  발견사항으로 보고.
