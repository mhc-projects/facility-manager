# 어드민 사업장 상세 모달 - 시설 데이터 흐름 다이어그램

## 현재 데이터 흐름 (문제 상황)

```
┌─────────────────────────────────────────────────────────────────┐
│                    admin/business/page.tsx                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. 사용자가 사업장 행 클릭                                     │
│     └─> handleBusinessClick(business)                           │
│         ├─> setSelectedBusiness(business)                       │
│         ├─> loadBusinessFacilitiesWithDetails(business_name)    │
│         │   │                                                     │
│         │   ├─> loadBusinessFacilities(businessName)            │
│         │   │   └─> useFacilityStats hook 호출                  │
│         │   │                                                     │
│         │   └─> fetch(`/api/facilities-supabase/${businessName}`)│
│         │       │                                                 │
│         │       ├─ [API 호출 성공]                              │
│         │       │  └─> response.json()                          │
│         │       │      └─> result: {                            │
│         │       │           success: true,                       │
│         │       │           data: {                              │
│         │       │             facilities: {                      │
│         │       │               discharge: [...],  ◄─────┐      │
│         │       │               prevention: [...]        │      │
│         │       │             },                         │      │
│         │       │             dischargeCount: N,         │      │
│         │       │             preventionCount: M         │      │
│         │       │           }                            │      │
│         │       │         }                              │      │
│         │       │                                        │      │
│         │       ├─ [데이터 변환 로직]                  │      │
│         │       │  │                                     │      │
│         │       │  ├─> facilityApiData = result.data    │      │
│         │       │  │                                     │      │
│         │       │  └─> transformedData = {              │      │
│         │       │       business: {...},                │      │
│         │       │       discharge_facilities:           │      │
│         │       │         facilityApiData               │      │
│         │       │           .facilities                 │      │
│         │       │           .discharge ─────────────────┘      │
│         │       │           .map(...),                         │
│         │       │       prevention_facilities: [...],          │
│         │       │       summary: {                             │
│         │       │         discharge_count: N,                  │
│         │       │         prevention_count: M,                 │
│         │       │         total_facilities: N + M              │
│         │       │       }                                       │
│         │       │     }                                         │
│         │       │                                               │
│         │       ├─ [조건 체크]                                │
│         │       │  if (result.success && result.data) {        │
│         │       │    setFacilityData(transformedData)  ✅      │
│         │       │  } else {                                     │
│         │       │    setFacilityData(null)  ❌                │
│         │       │  }                                            │
│         │       │                                               │
│         │       └─ [에러 처리]                                │
│         │          catch (error) {                             │
│         │            setFacilityData(null)  ❌                │
│         │          }                                            │
│         │                                                       │
│         └─> setIsDetailModalOpen(true)                         │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ facilityData prop 전달
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│             components/business/modals/                          │
│                  BusinessDetailModal.tsx                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  "측정기기 및 네트워크" 섹션 렌더링:                            │
│                                                                   │
│  {facilityLoading ? (                                            │
│    <div>시설 정보를 불러오는 중...</div>  🔄                    │
│  ) : facilityData ? (  ◄───────── 여기서 null 체크             │
│    <>                                                             │
│      {/* Facility Summary Card */}                               │
│      <div className="bg-gradient-to-r from-blue-50...">          │
│        배출시설: {facilityData.summary.discharge_count}          │
│        방지시설: {facilityData.summary.prevention_count}         │
│        배출구: {계산된 배출구 개수}                             │
│      </div>                                                       │
│    </>                                                            │
│  ) : (                                                            │
│    <div>                                                          │
│      등록된 대기필증 정보가 없습니다  ❌ ◄─ 문제 발생 지점     │
│      시설 정보를 확인하려면 먼저 대기필증을 등록하세요          │
│    </div>                                                         │
│  )}                                                               │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 문제 발생 가능한 지점

### 지점 1: API 응답 실패
```
fetch(`/api/facilities-supabase/${businessName}`)
  ├─ [네트워크 오류] ❌
  │  └─> catch (error) → setFacilityData(null)
  │
  ├─ [API 500/503 오류] ❌
  │  └─> response.ok = false → setFacilityData(null)
  │
  └─ [API 응답: success: false] ❌
     └─> else 블록 → setFacilityData(null)
```

### 지점 2: 데이터 구조 불일치
```
result.data = {
  facilities: {
    discharge: undefined,  ❌  (예상: 배열)
    prevention: undefined  ❌  (예상: 배열)
  }
}
  └─> facilityApiData.facilities.discharge.map(...)
      └─> TypeError: Cannot read property 'map' of undefined
          └─> catch (error) → setFacilityData(null)
```

### 지점 3: 빈 시설 데이터
```
result.data = {
  facilities: {
    discharge: [],  ⚠️  (빈 배열)
    prevention: []  ⚠️  (빈 배열)
  },
  dischargeCount: 0,
  preventionCount: 0
}
  └─> transformedData = {
      discharge_facilities: [],  ⚠️  (빈 배열)
      prevention_facilities: [],  ⚠️  (빈 배열)
      summary: {
        discharge_count: 0,
        prevention_count: 0,
        total_facilities: 0
      }
    }
  └─> setFacilityData(transformedData)  ✅ (데이터는 설정됨)
  └─> BusinessDetailModal 렌더링:
      facilityData ? (
        summary.total_facilities === 0 이면 빈 카드 표시  ⚠️
      )
```

## 해결 후 기대 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                    admin/business/page.tsx                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  loadBusinessFacilitiesWithDetails(businessName)                 │
│    │                                                              │
│    ├─> ✅ 로깅 추가:                                            │
│    │   console.log('🔍 [FACILITY-LOAD] 조회:', businessName)    │
│    │   console.log('📡 [FACILITY-LOAD] 응답:', result)          │
│    │   console.log('✅ [FACILITY-LOAD] 변환:', transformedData)  │
│    │                                                              │
│    ├─> ✅ 조건문 개선:                                          │
│    │   if (result.success && result.data && result.data.facilities) {│
│    │     // 시설이 없어도 빈 배열로 변환                         │
│    │     const transformedData = {                               │
│    │       discharge_facilities: (facilities.discharge || []).map(...),│
│    │       prevention_facilities: (facilities.prevention || []).map(...),│
│    │       summary: { ... }                                       │
│    │     }                                                        │
│    │     setFacilityData(transformedData)  ✅                   │
│    │   }                                                          │
│    │                                                              │
│    └─> ✅ 에러 핸들링 개선:                                     │
│        catch (error) {                                           │
│          console.error('❌ [FACILITY-LOAD] 실패:', error)       │
│          setFacilityData(null)                                   │
│        }                                                          │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ facilityData prop
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│             BusinessDetailModal.tsx                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ✅ 조건문 개선:                                                │
│  {facilityLoading ? (                                            │
│    <div>시설 정보를 불러오는 중...</div>  🔄                    │
│  ) : facilityData &&                                             │
│       (facilityData.summary.total_facilities > 0 ||              │
│        facilityData.discharge_facilities.length > 0 ||           │
│        facilityData.prevention_facilities.length > 0) ? (        │
│    <>                                                             │
│      {/* 시설 정보 표시 */}                                     │
│      <div>배출시설: {facilityData.summary.discharge_count}</div> │
│      <div>방지시설: {facilityData.summary.prevention_count}</div>│
│      <div>배출구: {계산된 개수}</div>                           │
│    </>                                                            │
│  ) : (                                                            │
│    <div>                                                          │
│      등록된 대기필증 정보가 없습니다  ⚠️                       │
│      (실제로 시설이 없는 경우에만 표시)                         │
│    </div>                                                         │
│  )}                                                               │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 디버깅 체크리스트

### 1. 브라우저 개발자 도구 콘솔 확인
```
예상 로그 출력:
✅ 🔍 [FACILITY-LOAD] 사업장 시설 정보 조회: 사업장명
✅ 📡 [FACILITY-LOAD] API 응답 상태: 200 true
✅ 📊 [FACILITY-LOAD] API 응답 데이터: { success: true, hasData: true, hasFacilities: true, dischargeCount: 2, preventionCount: 3 }
✅ ✅ [FACILITY-LOAD] 변환 완료: { dischargeCount: 2, preventionCount: 3, totalFacilities: 5 }

오류 시 로그:
❌ 📡 [FACILITY-LOAD] API 응답 상태: 500 false
❌ ⚠️ [FACILITY-LOAD] API 응답 데이터 형식 오류: { success: false, hasData: false }
❌ ❌ [FACILITY-LOAD] 사업장 시설 정보 로드 실패: Error: ...
```

### 2. 네트워크 탭 확인
```
Request:
  GET /api/facilities-supabase/%EC%82%AC%EC%97%85%EC%9E%A5%EB%AA%85

Response:
  Status: 200 OK
  Body: {
    "success": true,
    "data": {
      "facilities": {
        "discharge": [...],
        "prevention": [...]
      },
      "dischargeCount": 2,
      "preventionCount": 3
    }
  }
```

### 3. React DevTools Props 확인
```
BusinessDetailModal
  └─ facilityData: {
      business: { id, business_name },
      discharge_facilities: [...],  // 배열 길이 확인
      prevention_facilities: [...],  // 배열 길이 확인
      summary: {
        discharge_count: 2,
        prevention_count: 3,
        total_facilities: 5
      }
    }
```

## 예상 결과

### Before (문제 상황)
```
[상세 모달 - 측정기기 및 네트워크 섹션]
┌──────────────────────────────────────┐
│ 측정기기 수량                        │
│ ✅ 배출전류계: 2                     │
│ ✅ 송풍전류계: 3                     │
│ ✅ 게이트웨이(1,2): 1                │
└──────────────────────────────────────┘
┌──────────────────────────────────────┐
│ ⚙️  등록된 대기필증 정보가 없습니다  │  ❌ 문제!
│    먼저 대기필증을 등록하세요        │
└──────────────────────────────────────┘
```

### After (해결 후)
```
[상세 모달 - 측정기기 및 네트워크 섹션]
┌──────────────────────────────────────┐
│ 측정기기 수량                        │
│ ✅ 배출전류계: 2                     │
│ ✅ 송풍전류계: 3                     │
│ ✅ 게이트웨이(1,2): 1                │
└──────────────────────────────────────┘
┌──────────────────────────────────────┐
│ 시설 정보 (대기필증 기준)            │  ✅ 정상 표시!
│ 배출시설: 2  방지시설: 3  배출구: 2  │
└──────────────────────────────────────┘
```
