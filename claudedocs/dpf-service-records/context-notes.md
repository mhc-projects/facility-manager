# 컨텍스트 노트 — DPF 서비스 이력(AS/크리닝/물류) (2026-09-11)

## 배경
DPF 담당자들이 익숙해하는 크린어스(제조사) DEAR System의 부착현황/물류관리/접수현황 화면 구조를 로그인해서 직접 조사하고,
facility-manager `/dpf` 모듈에 이식할 설계를 작성함. 전체 설계는 `../dpf-as-logistics-design.md` 참고.

## 핵심 결정 (사용자 확정)
- 본사/협력사 접수경로 구분 없음 — 블루온은 콜센터가 전화 전부 직접 받음.
- 크린어스는 vendor enum에 지금 추가 안 함 (fujino/mz 유지).
- 첨부파일(12슬롯) 구조는 1단계에서 만들지 않음 — 담당자 요구 구체화되면 5단계로.
- 연장 워크플로우는 전자결재 미연동, 단순 boolean 필드.
- 회차(round_no)는 전환(크리닝↔AS) 시 새 category 기준 재채번, 원래 분류는 `converted_from_category`에만 흔적.
- 변경정보 오버레이는 `dpf_vehicles`에 컬럼 추가 방식(파생 계산 아님) — 부착현황 목록에서 조인 없이 보여주기 위함.

## 어드바이저 검토에서 나온 수정 (2026-09-11, 구현 전)
1차 설계안을 어드바이저에게 검토받아 6개 필수 수정 + 5개 권장 수정을 반영함 (설계 문서 각 절에 타임스탬프로 표시돼 있음).
- **round_no 채번을 API가 아니라 DB 트리거로**: Supabase JS 호출에서 "MAX 조회→+1→INSERT"를 하면 동시 접수 시 경쟁 상태 발생.
  전자결재 approve/submit 라우트가 `lib/supabase-direct`의 `transaction()` 헬퍼를 빠뜨려 부분 업데이트 버그가 났던 전례가 있어서
  ([[project_approval_pending_fixes]] 참고), API 코드가 매번 기억해야 하는 방식 대신 DB 트리거로 강제함.
- **오버레이 write-back 대상 재정리**: 1차 초안은 `current_vin_override`까지 자동 write-back한다고 적었지만, 접수 등록 폼에
  차량번호/차대번호 입력란이 아예 없어서(§2.2 관찰 내용에 없음) 자동화할 소스가 없었음. `current_plate_number`/`current_vin_override`/
  `is_special_management`/`is_special_sale`은 전부 수동 전용(VehicleFormModal), 연락처·출동지역만 자동 write-back으로 수정.
- **처리내용(processing_content) 컬럼 누락 발견** — 내역 테이블의 3번째 텍스트 필드가 스키마에 빠져 있었음. 추가.
- **삭제≠취소**: 협회청구일자를 들고 있는 원장을 하드 삭제하면 안 됨. `is_deleted` 소프트 삭제 + `status`에 `cancelled` 추가.
- **지자체/처리점은 자유 텍스트**: 크린어스의 코드 테이블(지자체 168개 등)은 우리 쪽에 없음. `local_gov_code`/`service_branch_id`를
  `local_government`/`service_branch` 자유 텍스트로 변경. 담당AS기사/처리기사도 1단계는 자유 텍스트, employees FK 연동은 후속 과제.
- **특판(is_special_sale)은 차량 속성**: 크린어스 필터에서 특판은 접수 건이 아니라 계약(차량) 단위 속성이었음 — `dpf_vehicles`로 이동.
- **1단계 API 라우트를 vin 스코프 서브레코드 패턴으로**: 처음엔 `/api/dpf/service-records` 최상위 엔드포인트를 1단계에 넣었는데,
  기존 `installations`/`inspections` 라우트와 패턴을 맞추는 게 맞음 — 최상위 통합 리스트 엔드포인트는 2단계(접수현황 페이지)로 이동.

## 미관찰 (구현 시 주의)
- 크린어스 "서류 보기" 상세 화면 — 클릭이 반응하지 않아 구조를 못 봄. 첨부파일 설계(§4.7)는 등록 폼의 파일 슬롯에서 유추한 것.
- 크린어스 "접수종류" 드롭다운 옵션 — 클릭 위치가 빗나가서 못 엶. 1단계에서는 `category` 자체를 접수종류로 취급.

## 마이그레이션 핸드오프 상태
`supabase/migrations/20260911_dpf_service_records.sql` 작성 완료(2026-09-11). 어드바이저 2차 검토에서 `current_vin_override`/
`current_plate_number` 타입이 설계 초안 그대로면 실제 데이터(비표준 VIN, 지역+번호 합산 차량번호)를 못 받는다는 지적을 받아
`20260424_dpf_vin_length.sql`을 확인하고 `dpf_vehicles.vin`(VARCHAR(20))/`plate_number`(VARCHAR(50))와 동일하게 맞춤.
트리거에 `converted_from_category` 자동 기록, `vehicle_id` UPDATE 방어, `reception_date DEFAULT CURRENT_DATE`도 같은 리뷰에서 추가.
**사용자에게 전달 전 상태 — 아직 실행 요청 안 함.** 전달 후 사용자가 Supabase SQL 에디터에서 직접 실행하고 결과(성공/에러)를
알려주면 그 다음(`types/dpf.ts`)으로 진행. 에이전트가 DB에 직접 붙어서 실행하지 않음([[feedback_supabase_sql]],
[[project_hardcoded_db_password_incident]] 때문에 자격 증명도 직접 다루지 않음).

## 1단계 사후 검토 (2026-09-11, 어드바이저)
1단계 완료 후 어드바이저에게 "깔끔하게 됐는지" 재검토 요청 — 결과: 설계한 규칙(트리거 채번, 전환, write-back 최신 레코드 규칙, 소프트 삭제)
전부 브라우저 검증으로 실제 확인됨, 커밋 분리도 계획대로. 잔여 3건:
1. 브라우저 검증 중 실제 차량(260132205738, 이채규 소유)에 테스트 데이터(접수 이력 2건 + 연락처 010-9999-0000)가 남음 —
   하드 삭제 SQL을 사용자에게 전달, 2026-09-11 사용자가 직접 실행 완료 확인. `is_deleted` 소프트 삭제(§4.10)와는 별개 — 이건 실제
   업무 데이터가 아니라 순수 테스트 흔적이라 하드 삭제가 맞다.
2. 설계문서 §7이 여전히 `PATCH`로 적혀 있었음(실제 구현은 PUT) — 문서만 안 맞았던 것, 수정함.
3. write-back은 구현했지만 그 값을 보여주는 화면이 없었음(`VehicleFormModal`/`BasicInfoTab` 둘 다 오버레이 필드 미노출) —
   1단계 체크리스트엔 없던 항목이라 결함은 아니지만, 2단계 범위로 명시적으로 추가함(`BasicInfoTab`에 읽기 전용 섹션).

## 2단계 계획 (2026-09-11, 어드바이저 검토로 구체화)
- 목록 API는 새로 설계하지 않고 `/api/dpf/search/route.ts`(기존 차량 검색 페이지네이션 라우트)의 쿼리 파라미터/응답 모양을 그대로 복제.
- 통계는 저장 카운터나 RPC 없이 6개 count 쿼리 병렬 호출로 충분(이 규모에서).
- 필터 UI도 새로 디자인하지 않고 `/dpf`(부착현황) 페이지의 검색행+필터패널+탭 패턴을 재사용.
- 접수현황 목록에 "빠른 등록" 버튼을 넣지 않기로 명시적으로 결정 — 크린어스도 등록은 차량 쪽에서만 하고, 신규 접수용 차량 선택 UI는
  3단계(부착현황 행 액션 버튼) 몫. 여기서 조급하게 만들면 3단계와 중복 설계하게 됨.
- 사이드바(`AdminLayout.tsx`)의 `DPF업무` 그룹은 현재 "차량 관리" 1개 항목뿐(중첩 없는 평면 구조) — "접수현황"을 형제 항목으로 추가.
  아이콘은 이미 import돼 있는 `ClipboardList` 재사용, 새 import 불필요.
- 마이그레이션 없음 — 2단계는 순수 조회 레이어. 필요하다고 느껴지면 멈추고 먼저 확인하기로 함.

## 2단계 구현 중 발견 (2026-09-11)
- **`.or()` 조인 필터 문법**: PostgREST의 `or=()` 구문은 와일드카드로 `%`가 아니라 `*`를 쓴다. `%`를 그대로 넣으면
  "failed to parse logic tree" 파싱 에러가 남(문서화되지 않아 실제 요청으로 확인). 임베디드 테이블(조인) 필터는 문자열에
  `테이블명.컬럼명` 접두어를 직접 넣는 대신 supabase-js의 `.or(filter, { foreignTable: '...' })` 2번째 인자를 쓰는 게 맞음.
  실제 요청(`?q=8787`, `?q=이채규`)으로 검증 완료.
- **dev 서버 청크 캐싱이 클라이언트 네비게이션 테스트를 오도할 수 있다**: `?tab=` 기능을 클라이언트 사이드 Link 클릭으로 테스트했을 때
  반복적으로 실패해서 useEffect까지 추가하며 원인을 찾았는데, 실제로는 로직이 처음부터 정상이었고 **같은 브라우저 탭에서 파일을
  수정하는 동안 이미 로드돼 있던 `/dpf/[vin]` 페이지의 JS 청크를 클라이언트 사이드 네비게이션이 재사용**해서 최신 코드가
  반영 안 되고 있었던 것. `navigate`(주소창 이동)로 시작 페이지를 하드 리로드해서 청크를 최신화한 뒤 그 상태에서 클릭 네비게이션을
  하면 정상 동작함을 확인. **[[feedback_local_test_hard_reload]]가 "dev 서버 재시작 후"만 언급하는데, 실제로는 서버 재시작 없이
  파일만 수정해도 같은 탭에서 클라이언트 사이드 네비게이션을 반복 테스트하면 같은 증상이 난다** — 이 메모리를 갱신할 가치가 있음.
  결과적으로 유지한 것: `useEffect`로 `searchParams` 변경 시 `activeTab`을 보정하는 로직(실사용자 시나리오에서도 Next.js
  라우터 캐시로 컴포넌트가 재사용되는 경우의 안전망으로 의미가 있어 남김), 디버그용 `data-debug-*` 속성은 제거함.

## 2단계 사후 검토 (2026-09-11, 어드바이저)
2단계 결과물 어드바이저 재검토 — "clean, 체크리스트 전부 실제 브라우저 증거로 검증됨, 커밋 분리도 정확함" 확인. 잔여 3건(블로커 아님):
1. 정리 SQL이 1단계 시점 기준이라 2단계 검증분까지 포함하는지 재확인 필요하다는 지적 — 확인 결과 2단계 검증은 1단계에서 만든
   레코드를 재사용했을 뿐 추가로 쓴 필드가 없어(무선 연락처만 입력) 기존 2줄 SQL로 충분함. 사용자가 2026-09-11 실행 완료.
2. `app/dpf/service/page.tsx`의 통계 재조회 `useEffect`가 `[result.total]`에 걸려 있어 필터 바뀔 때마다(=목록 건수가 바뀔 때마다)
   전역 통계를 다시 조회함 — 지금 규모에선 무해하지만 의미상 필터(로컬)와 통계(전역)를 섞은 것. 3단계에서 이 페이지에 등록 버튼이
   생기면 "마운트 시 1회 + 등록 성공 시에만" 재조회로 좁히는 걸 검토. 2단계 결함은 아니라 지금은 고치지 않음.
3. `initialTab`과 보정용 `useEffect`가 같이 있는 이유를 주석으로 남겨둠(page.tsx:62,71) — 둘 다 필요(초기 렌더 vs 라우터 캐시 재사용)함을
   다음 세션이 실수로 하나를 지우지 않도록 명시.

## 3단계 계획 (2026-09-11, 어드바이저 3차 검토로 구체화)
- **성능**: `/dpf` 목록에 크리닝횟수/AS횟수 등을 파생 컬럼으로 붙이는 게 "조회 레이어 추가"처럼 보이지만, 실제로는 페이지당
  20건 × 3개 소스 테이블 서브쿼리가 될 위험이 있었음 — 어드바이저가 "성능 결정을 UI 결정처럼 숨기지 말라"고 지적. 메인 목록
  쿼리(`/api/dpf/search`)는 건드리지 않고, 현재 페이지에 보이는 vehicle_id만 모아 별도 배치 조회 API 1개
  (`GET /api/dpf/service-records/derived-stats`)를 새로 부른다. PostgREST에 GROUP BY가 없어서 집계는 서버 응용 코드(JS)에서 한다.
- **컬럼 예산**: `DpfVehicleTable`가 이미 19컬럼·`minWidth 1780px`이라 6개를 그대로 더하면 25개가 된다 — 크린어스도 부착현황에서
  일부 컬럼(주소, 이전업체명)을 뺐던 것으로 관찰됨(§2.1). 기존 19개 컬럼은 그대로 두되(담당자가 실제 쓰는지 확인 없이 임의로
  빼지 않음), 신규는 3개(접수현황=최근접수일/마지막처리일 2줄 스택, 처리횟수=`크N·ASN·철N` 압축, 부착경과일)+액션버튼 1개 =
  4개만 추가(23컬럼, minWidth ~2150px)로 압축.
- **철부횟수 벤더 처리**: `dpf_device_installations`가 엠즈 차량에는 애초에 안 채워진다는 걸 git log(103ec33, "엠즈 형식 임포트
  지원") 커밋 메시지와 `ALL_TABS`의 `installation`/`inspection`/`subsidy` 탭이 전부 `vendors: ['fujino']`로 제한된 것으로
  교차 확인 — 엠즈는 자체 DB 파일을 가져와 차량 마스터만 채우고 설치이력 서브테이블은 임포트하지 않는다. 그래서 API는 벤더를
  따로 안 걸러도 엠즈 차량은 항상 0이 돌아오지만, "실제 철거 0회"와 "애초에 추적 안 함"을 화면에서 구분하려고 **클라이언트에서만**
  `vendor==='mz'`이면 `-`로 덮어쓰기로 결정. 이 판단은 DB에 직접 접속해 확인한 게 아니라 커밋 이력 교차검증으로 내린 것이라,
  구현 중 실제 엠즈 차량 1대로 브라우저 검증할 때(체크리스트) 다시 한번 눈으로 확인한다.
- **재조회 패턴**: 2단계 사후검토에서 `/dpf/service/page.tsx`의 통계 `useEffect`가 `[result.total]`에 걸려 필터 바뀔 때마다
  전역 통계를 다시 도는 걸 지적받았음(§2단계 사후 검토). 3단계 신규 코드에서 같은 실수를 반복하지 않기 위해, 파생통계 fetch를
  별도 `useEffect`로 만들지 않고 `search()` 콜백 안에서 목록 fetch 직후 바로 호출하도록 명시적으로 결정.
- **버튼→모달**: `ServiceRecordFormModal`은 1단계에서 이미 `initialCategory?: DpfServiceCategory`를 받아서 수정 불필요.
  "접수" 버튼은 `initialCategory` 생략(폼 드롭다운에서 직접 선택), "물류" 버튼은 `'parts_delivery'`로 프리셋(폼 안에서
  `urea`로 바꿀 수 있음) — 크린어스의 "접수상담"/"엔진물류" 두 버튼과 같은 구분이지만, 우리는 폼을 하나로 통합했으므로
  차이는 초기 선택값뿐이다(별도 스트립다운 폼 없음).
- `DpfVehicleTable`는 `app/dpf/page.tsx` 한 곳에서만 쓰임을 그렙으로 확인 — 액션 버튼을 prop으로 게이팅할 필요 없음.

## 3단계 계획 4차 검토 (2026-09-11, 어드바이저) — 3차 검토 후 4가지 추가 반영
1. **취소 건이 횟수에 섞이면 안 됨**: `clean_count`/`as_count`를 처음엔 `status` 무관하게 셌는데, 2단계 접수현황 요약 바가
   `cancelled`를 대기/완료 집계에서 빼는 것과 숫자가 어긋나면 담당자가 혼란스러워한다는 지적 — `status != 'cancelled'`만
   세도록 §7 수정. `last_reception_date`는 취소 건도 포함(접수 자체는 실제 발생).
2. **로딩 중간 상태를 0으로 보여주면 안 됨**: 목록 fetch와 파생통계 fetch가 순차라서 `derivedStats[id]===undefined`인
   구간이 항상 생김 — "기록 없음(API가 준 0)"과 "아직 안 옴(undefined)"을 구분해서 후자는 `…`로 표시하기로 결정.
3. **"부착경과일" 라벨이 실제로는 구변일 기반**: `installation_date`가 `lib/dpf-column-map.ts`에서 "구변일자/구조변경일자"에
   매핑됨을 확인(그렙으로 검증, DB 직접 조회 아님) — 크린어스의 "부착경과일"은 물리적 부착일이라는 별개 개념이고 우리
   스키마엔 없으므로, 같은 이름을 쓰지 않고 "구조변경경과일"로 명명 변경.
4. **컬럼 레이아웃 재검토**: "접수현황" 2줄 스택 컬럼은 그 행만 키가 달라져 세로 정렬이 깨지므로 포기하고 최근접수일/
   마지막처리일을 단일 라인 별도 컬럼 2개로 분리 — 결과적으로 신규 컬럼이 3개가 아니라 4개(최근접수일/마지막처리일/
   처리횟수/구조변경경과일)+액션버튼 1개 = 24컬럼(23→24로 수정).

## 3단계 구현/검증 결과 (2026-09-11)
설계 그대로 구현, 브라우저 검증에서 재설계가 필요한 문제는 없었음.
- 배치 조회 API를 테이블에 붙이기 전에 `javascript_tool` fetch로 단독 호출해 응답 모양부터 확인(2단계와 동일 절차) —
  테스트 차량 3건(크리닝 완료 1건 + AS 진행중 1건 + 크리닝 취소 1건)을 만들어 넣고 정확히 `clean_count=1(취소 제외)`,
  `last_reception_date`=취소 건 포함 최댓값, `last_processed_date`=취소 건 포함 최댓값으로 나오는 것을 API 레벨에서 먼저 검증.
- 테이블에 붙인 뒤 실제 상세 탭(`/dpf/[vin]?tab=service`)의 4건(크리닝 3회차 진행중/크리닝 2회차 취소/AS 1회차 진행중/
  크리닝 1회차 완료)과 목록 파생값(크2·AS1·철0, 최근접수일 2026-09-11, 마지막처리일 2026-09-09)이 정확히 일치함을 확인 —
  집계 로직이 실제 데이터로도 설계 의도대로 맞아떨어짐.
- 엠즈 차량(KL2UL61FDYP002208)에서 최근접수일/마지막처리일이 `—`(기록 없음), 철부횟수가 `-`(벤더상 미추적) — 각각 다른
  이유의 대시(dash)이지만 화면상 구분은 없음. 담당자가 실제로 헷갈려하면 후속으로 툴팁 등을 고려할 수 있으나, 지금은
  범위 밖(요청받지 않은 개선)으로 판단해 그대로 둠.
- "접수" 버튼(분류 미선택 상태로 모달 오픈) / "물류" 버튼(`parts_delivery` 프리셋)이 설계대로 각각 다르게 열리는 것,
  등록 후 `onSuccess`가 모달을 닫고 `search()`를 재호출해 목록+파생값이 함께 갱신되는 것(등록 직후 크1→크2로 즉시 반영)을
  실제 클릭으로 확인.
- 구조변경경과일 계산(UTC 날짜 부분만 비교)은 엠즈 차량(구변일 2006-07-06)에서 7372일, 테스트 차량(구변일 2022-06-16)에서
  1548일로 표시 — 어드바이저 4차 검토에서 수기 검산으로 둘 다 일치 확인(2022-06-16→2026-06-16=1461일 + 6/17~9/11=87일=1548,
  타임존 드리프트 없음).

## 3단계 마무리 검토 (2026-09-12, 어드바이저)
어드바이저가 3단계를 "증거 기반으로 정확함"이라 확인하면서 마무리 전 처리할 3가지를 지적:
1. 커밋 4개 중 마지막(체크리스트 해시 반영)에 Co-Authored-By/Claude-Session 트레일러 누락(헤레독 작성 실수) — 사용자에게
   확인 후 amend로 수정(아직 미푸시라 안전).
2. `DpfVehicleTable`의 `<table minWidth>`가 1780px 그대로였음(설계 문서는 ~2250px라고 적어놓고 코드는 안 고침) — 실제로는
   `<th>` 개별 width 합(~2630px)이 테이블을 밀어서 화면상 문제는 없었지만, 문서와 코드가 어긋나 있던 것 자체가 결함 —
   2250px로 수정.
3. 후지노(FJ) 벤더 탭 확인이 디바운스 완료 전 스크린샷이라 "전체"와 숫자가 같아 보여 결론 없음(inconclusive) — 아래에서 재검증.
4. (비차단) `.claude/skills/`에 `dpf_service_records`를 다루는 파일이 `db-schema` 하나뿐 — 설계 §10이 1단계 완료 시 처리하기로
   했던 도메인 스킬 갱신이 3단계까지 누락돼 있었음. `.claude/skills/dpf/SKILL.md` 신설로 처리(round_no 트리거, 전환 재채번,
   write-back 최신 레코드 규칙, 삭제≠취소, 파생 컬럼 배치 조회 원칙, 엠즈 설치이력 공백, 자유 텍스트 필드).

## 4단계 계획 (2026-09-12, 어드바이저 검토로 구체화)
- **추출 vs 복제**: `/dpf/service/page.tsx`(~250줄)를 물류관리용으로 그대로 복제하면 필터 패턴 하나 바뀔 때마다 두 파일을
  고쳐야 한다 — 로직스틱스가 이 화면의 두 번째 소비자가 되는 시점이고, §8.2 설계도 애초에 "8.1과 컴포넌트 공유"라고
  적어뒀으므로 지금이 추출할 시점. `DpfServiceListView.tsx`(mode prop)로 추출.
- **로직스틱스 분류 필터의 "전체"가 진짜 전체면 안 됨**: `category` state가 빈 문자열일 때 API에 아예 안 보내면 6개
  카테고리 전부(AS/크리닝/상담종료까지) 돌아온다 — 로직스틱스 화면에서 "전체"는 부품전달+요소수 두 개의 전체여야 하므로
  `effectiveCategory = mode==='logistics' ? (category || 'parts_delivery,urea') : category` 규칙으로 서버에 보내는 값을
  항상 이 우주 안으로 강제한다.
- **필터 슬롯 교체 근거**: 처리점(service_branch)은 크린어스의 엔진물류 등록 폼(§2.3)에 애초에 없는 필드라 로직스틱스에서
  필터로 노출하는 게 의미 없음(빈 결과만 나옴) — 대신 택배사(courier)를 그 자리에 넣는다. 비용(cost_type) 필터는 크린어스
  §2.4 관찰에만 있고 §8.1(reception)에는 없던 것이라 logistics 전용으로만 추가, reception 페이지는 안 건드린다.
- **`/stats` 확장 vs 분리**: 새 엔드포인트나 `?scope=` 대신 기존 응답에 4필드를 얹기로 결정 — 접수현황/물류관리가 결국
  "같은 원장의 필터 뷰"라는 §2.6 설계 원칙과 맞고, count 쿼리 5→9개 정도는 2단계 때와 같은 근거로 문제없다.
  reception 페이지는 새 4필드를 그냥 무시하고, logistics 페이지는 기존 5필드를 무시한다.
- **통계 useEffect 수정 시점**: 2단계 사후검토(§2단계 사후 검토 2번)에서 지적됐던 `[result.total]` 의존성 문제를 지금
  고친다 — 어차피 이 블록을 `DpfServiceListView`로 옮겨 적어야 하니 다시 쓰는 김에 마운트 1회(`[]`)로 단순화. 이 페이지엔
  등록 버튼이 없어(§8.1 "빠른 등록 없음" 결정) 로컬 변이로 `result.total`이 바뀔 일이 없으므로 안전한 수정이다.
- **탭 라벨 변경 근거**: `/dpf/[vin]`의 "AS/크리닝" 탭은 1단계부터 카테고리 무관하게 해당 차량의 전체 서비스 이력을
  보여주고 있었다(물류 포함) — 이름과 내용이 처음부터 어긋나 있었지만 물류가 전용 화면 없이 묻혀 있을 땐 눈에 덜 띄었다.
  4단계로 물류가 첫 번째 화면(`/dpf/service/logistics`)을 갖게 되면 이 불일치가 더 두드러지므로 라벨만 "접수이력"으로
  바꾼다(내용/API 변경 없음).

## 4단계 구현/검증 결과 (2026-09-12)
- 코드 작성 전 어드바이저 그렙 요청 2건 처리: (1) "AS/크리닝" 문자열이 `ALL_TABS`뿐 아니라 `TabHeader`(섹션 헤더)에도
  있어서 둘 다 바꿈, 모달 타이틀 폴백은 접수상담/엔진물류 구분을 반영하는 것이라 그대로 둠. (2) `AdminLayout`의 활성
  탭 판정이 이미 `hasMoreSpecificMatch`로 `/dpf/service`와 `/dpf/service/logistics`(하위 경로) 이중 하이라이트를 방지하고
  있음을 확인 — 별도 처리 불필요.
- 실제 검증 전 테스트 데이터가 하나도 없어서(3단계 정리 SQL 실행 후) 필터/요약 검증이 공집합에서는 무의미하다는 지적에
  따라, 부품전달(진행중, 경동화물, 유상) + 요소수(완료, 대신화물, 무상) 2건을 먼저 만들어 넣고 검증 — 요약 4타일
  (요소수완료 1/부품전달대기 1), 택배사 필터("경동" → 1건), 분류 드롭다운(부품전달/요소수 2개만) 전부 실제 데이터로 확인됨.
- `DpfServiceRecordTable`의 `SkeletonRow`/`<table minWidth>`가 원래 컬럼 배열에 하드코딩돼 있던 것을, variant 2개를 쓰게
  되면서 컬럼 배열 기반으로 동적 계산하도록 함께 고침 — 3단계에서 "설계 문서는 바뀌었는데 코드의 minWidth 상수는 안
  바뀌어 있던" 실수를 구조적으로 재발 방지(하드코딩된 숫자 자체를 없앰).
- reception 페이지(`/dpf/service`) 회귀를 물류 작업보다 먼저 확인 — 추출(extraction)이 가장 위험한 지점이라는 어드바이저
  지적대로, 커밋 (b) 직후 하드 리로드로 기존 화면(요약 5타일/필터 패널/테이블 컬럼)이 그대로임을 먼저 확인하고 나서
  (c)(물류 신규 화면)로 넘어감.

## 5단계 계획 (2026-09-12, 사용자 확인 + 어드바이저 검토로 구체화)
- **착수 조건 확인**: 1단계에서 "담당자 요구가 구체화되면"이라는 조건으로 보류했던 항목이라, 4단계 완료 후 "그 조건이
  실제로 충족됐는지" vs "일단 크린어스가 관찰한 12슬롯 그대로 만들지"를 AskUserQuestion으로 확인 — 사용자가 후자를
  선택함(2026-09-12). 조건이 충족됐다고 임의로 판단하지 않고 명시적으로 물어본 것.
- **버킷 사전조치 불필요 확인**: §4.7 초안이 "`dpf-documents` 버킷 재사용"이라고만 적어뒀는데, 그 버킷이 실제로 이미
  존재하는지 이 세션에서 DB를 직접 조회해 확인할 수는 없었다(DB 직접 접속 금지 원칙) — 대신 `grep -rn "dpf-documents"`로
  `app/api/wiki/upload-guideline/route.ts`가 이미 이 버킷명을 쓰면서 "없으면 자동 생성" 코드를 갖고 있음을 확인해,
  같은 자동생성 패턴을 우리 업로드 라우트에도 그대로 넣으면 사용자가 Supabase 대시보드에서 버킷을 미리 만들어둘
  필요가 없다고 결론 — 두 번째 사용자 액션(버킷 생성)이 생기는 걸 코드 패턴 재사용으로 피함.
- **하드 삭제 vs 소프트 삭제**: `dpf_service_records`의 `is_deleted`(§4.10)는 협회청구 이력 보존이 목적이었다 — 첨부파일은
  청구와 무관하고 재업로드로 언제든 대체 가능하므로, 첨부파일 삭제는 하드 삭제로 결정(레코드 자체의 소프트 삭제 원칙과
  다른 축이라는 걸 명시적으로 구분).
- **일괄 업로드/서류 컬럼 안 만듦**: 크린어스가 실제로 파일명↔슬롯을 어떻게 매칭하는지("일괄 업로드" 관찰) 및 목록의
  "서류" 컬럼 클릭 시 무엇이 열리는지(§6-3) 둘 다 미관찰이라, 추측해서 구현하지 않고 개별 업로드+모달 내 섹션으로만
  범위를 한정.

## 5단계 계획 2차 검토 (2026-09-12, 어드바이저) — 마이그레이션 작성 전 2건 수정
1. **`storage_path` 컬럼 누락**: 최초 스키마 초안엔 `file_url`(공개 URL)만 있었는데, 재업로드 시 이전 파일을 지우려면
   실제 storage 경로가 필요하고 서명 URL 방식으로 바꾸면서도 경로 컬럼이 필수가 됐다. `file_url` 대신 `storage_path`로
   교체.
2. **버킷 공개여부를 베낀 채로 넘어갈 뻔함**: `app/api/wiki/upload-guideline/route.ts`의 "공개 버킷" 패턴을 그대로
   복사해서 초안에 넣었는데, 그 라우트는 지침서 PDF(민감정보 없음)용이고 이번 첨부파일은 차량번호·소유자명이 찍힌
   차량 개인정보다 — 데이터 성격이 다른데 코드 패턴만 베꼈던 것. `grep -rn "createSignedUrl"`로 이 프로젝트에 이미
   비공개 버킷+서명 URL 패턴(`announcements`, `uploaded-files-supabase`)이 있음을 확인하고, 그걸 재사용하는 것으로
   결정 변경 — 새 버킷 `dpf-attachments`(비공개)를 만든다. **패턴을 재사용할 때 그 패턴이 전제하는 조건(데이터 민감도)
   까지 같이 복사되는지 매번 확인해야 한다는 교훈.**
3. (부수) `GET` 응답 필드명이 §7 초안엔 `uploaded_at`, §5 컬럼은 `created_at`으로 서로 달랐던 것을 `created_at`으로 통일.
4. (부수) `upload-guideline/route.ts`엔 `requireAuth`가 아예 없다는 걸 그렙으로 발견 — 이 라우트를 참고하면서 인증
   누락까지 따라가지 않도록 §7에 "requireAuth 먼저 호출"을 명시적으로 적어둠.

## 5단계 계획 3차 검토 (2026-09-12, 어드바이저) — 마이그레이션 확정 전 마지막 2건
1. **`uploaded_by` FK 드롭 여부 확인**: `dpf_service_records.created_by`도 같은 `employees(id)` FK를 쓰는데 실제로
   FK 위반 없이 동작해왔는지 근거가 필요하다는 지적 — `lib/auth/require-auth.ts`를 읽어 `requireAuth()`가 `employees`
   테이블에 실존하는 행만 통과시킴(`is_active=true` 조건까지 확인 후 없으면 forbidden)을 코드로 확인. FK를 그대로
   유지하기로 결정(드롭 불필요, 근거 있는 판단으로 전환).
2. **버킷 자동생성에 크기 제한 없음**: `createBucket(name, {public:false})`만 쓰면 Supabase 프로젝트 기본 한도(수십MB)가
   적용돼 클라이언트 10MB 가드를 우회한 업로드가 가능했다 — `fileSizeLimit: 10*1024*1024`를 버킷 생성 옵션에 추가해
   서버 쪽에서도 같은 숫자를 강제하도록 수정.

## 5단계 구현 중 발견 (2026-09-12) — 브라우저 확장이 파일 업로드 API 테스트를 차단함
API 레벨에서 먼저 검증하는 기존 관례(1~4단계 전부)를 따르려 했으나, `javascript_tool`로 `input.files[0]`를 읽어
`fetch`의 `FormData` body로 보내는 패턴 자체가 Claude-in-Chrome의 보안 필터에 걸려 `[BLOCKED: Cookie/query string data]`로
차단됐다 — 파일 내용(합성 canvas PNG, base64 디코딩 PNG, PIL로 새로 생성한 PNG 전부)이나 생성 방식과 무관하게 매번
차단됨, 반면 파일이 아예 없는 일반 JSON POST/GET은 계속 정상 동작했다. 결론: "스크립트가 file input의 실제 선택된
파일을 읽어 네트워크로 전송"하는 패턴 자체를 막는 안전장치로 보인다(같은 오리진으로 보내는 정당한 업로드인지 여부와
무관하게) — 이건 우회하려 하지 않고 그대로 받아들였다. **대신 파일이 포함되지 않는 케이스(빈 레코드 GET, 404, slot_key
검증, 미존재 첨부 DELETE)만 API 단독으로 먼저 검증**하고, 실제 업로드/재업로드/삭제/서명URL 만료 흐름은 UI를 완성한 뒤
`file_upload` 도구로 실제 `<input type=file>`에 파일을 선택시키고 페이지 자신의 React 코드(내가 직접 짠 스크립트가 아닌
이미 배포된 onChange 핸들러)가 fetch를 수행하게 해서 검증하기로 결정 — 이 경로는 차단되지 않을 가능성이 높다(차단은
`javascript_tool`에 직접 제출한 코드를 스캔하는 것으로 보이므로).

## 5단계 UI 구현 + 실사용 버그 발견 (2026-09-12)

### 브라우저 확장 차단 가설 — 반증됨
이전 섹션에서 "스크립트가 file input의 선택된 파일을 읽어 fetch로 전송하는 패턴 자체를 차단하는 것으로 보인다"고
추정했으나, UI 완성 후 `file_upload` 도구로 실제 `<input type=file>`에 파일을 선택시키고 페이지 자신의 React
onChange 핸들러가 fetch를 수행하게 하자 **정상적으로 서버까지 요청이 도달했다**(차단되지 않음). 즉 이전 관찰은
"이 기능 자체가 파일 업로드를 못 한다"는 뜻이 아니라 "`javascript_tool`에 직접 제출한 스크립트가 파일을 읽어
전송하는 패턴만 차단된다"는 좁은 범위의 현상이었다 — 실제 배포된 UI 흐름에는 해당하지 않는다. 이 구분을 명확히
해둔다: **일반적인 파일 업로드 기능 자체는 이 프로젝트에서 정상 동작하며, 막힌 것은 어디까지나 에이전트가 직접
작성한 스크립트의 fetch 호출뿐이었다.**

### 진짜 원인 — `supabaseAdmin`의 전역 Content-Type 헤더
실제 업로드가 415 "mime type application/json; charset=utf-8 ... is not supported"로 실패했다. 처음엔
`request.formData()`가 반환하는 `File`이 storage-js의 `fileBody instanceof Blob` 체크와 다른 realm이라 인식되지
않아 JSON으로 오인 직렬화된다는 가설을 세우고 `new Blob([...])`로 재포장했으나 **동일하게 실패**했다. 디버그
로그(`file instanceof Blob`, `blob instanceof Blob`, `Object.prototype.toString.call(blob)`)를 임시로 추가해
찍어보니 `blobIsBlob: true`로 정상 인식되고 있어 이 가설은 반증됐다.

진짜 원인은 `lib/supabase.ts`의 `getSupabaseAdmin()`이 `global: { headers: { 'Content-Type': 'application/json; charset=utf-8' } }`를
고정해두고 있다는 것이었다. storage-js는 Blob body를 FormData로 감싸 보내는데, 이때 자체적으로 Content-Type
헤더를 설정하지 않고 fetch의 자동 멀티파트 boundary 부여에 맡긴다 — 그런데 Fetch 스펙상 `init.headers`에 이미
`Content-Type`이 명시돼 있으면 이 자동 부여가 무시된다. 결과적으로 서버는 "Content-Type: application/json"이라고
선언된 요청에 실제로는 멀티파트 바이트가 온 것을 받아 415로 거부한다(sniff 실패로 두 번째 mime 값도 못 붙임).
raw ArrayBuffer로 시도했던 첫 실패("mime type application/json..., image/jpeg is not supported"라는 콤마 결합
값)도 같은 뿌리 — else 분기가 `headers['content-type']`(소문자)를 명시적으로 덧붙이면서 대소문자가 다른 키가
같은 객체에 공존하게 되고, Fetch의 `Headers` 생성자는 같은 이름(대소문자 무시)이 두 번 오면 값을 콤마로 합친다.

**중요한 정정**: `announcements/[id]/attachments/route.ts` 등 "기존 선례"로 참고했던 라우트들도 전부 같은
`getSupabaseAdmin()`을 통해 storage에 접근한다 — 즉 이 헤더 충돌 버그는 이번에 새로 만든 코드만의 문제가 아니라
프로젝트 전반에 잠재된 문제였다(단, 그 라우트들이 버킷에 `allowedMimeTypes` 제한을 두지 않아 415가 발생하지 않았을
뿐, 저장된 객체의 `mimetype` 메타데이터는 똑같이 오염됐을 가능성이 있다).

**수정**: `lib/supabase.ts`에 `global.headers` 없는 `getSupabaseStorageAdmin()`을 신규(lazy, additive)로 추가하고,
첨부파일 API 3개 라우트의 storage 호출(`listBuckets`/`createBucket`/`upload`/`createSignedUrl`/`remove`)만
이걸로 교체했다. DB 쿼리는 `Prefer: return=representation`이 필요해 기존 `supabaseAdmin`을 그대로 쓴다. 기존
`global.headers`나 다른 업로드 라우트는 건드리지 않았다([[feedback_phased_rollout_live_system]] — 광범위 수정은
무위험분만 즉시 반영, 나머지는 목록으로 사용자에게 보고).

### 서명 URL의 CDN 캐시 — old URL이 삭제 후에도 한동안 200을 반환함
재업로드 검증 중 `new Image()`/`fetch(url, {cache:'no-store'})`로 이전 서명 URL을 확인했더니 storage에서 이미
삭제된 뒤에도 200이 나왔다. storage list API로 실제 파일 개수를 직접 조회해보니 새 파일 1개만 존재해 실제 삭제는
정상 수행된 것으로 확인 — storage-js 업로드 기본값 `cacheControl: max-age=3600`을 Supabase Storage 앞단 CDN이
그대로 지켜 엣지에 캐시된 이전 응답을 계속 서빙한 것이었다. **교훈: 서명 URL의 HTTP 상태코드로 삭제 여부를
검증하는 건 신뢰할 수 없다(최대 1시간 CDN 캐시) — DB 행 존재 여부 또는 storage list API가 유일하게 신뢰 가능한
진실이다.** 부수 효과로, 교체/삭제된 PII 파일이 이전 서명 URL로 최대 1시간 더 열람 가능하다는 특성이 있다(TTL로
자연히 소멸하므로 별도 수정 대상은 아니고, 알려진 특성으로만 기록).

### 검증 완료 항목
이미지 업로드(썸네일 렌더, `img.complete && naturalWidth>0`) → 재업로드/교체(새 서명 URL 확인, storage list API로
이전 파일 삭제 확인) → PDF 업로드(`ext:"pdf"` 응답 확인, "PDF 보기" 링크 렌더링) → UI 삭제 버튼(storage+DB 모두
제거를 storage list API/REST 조회로 직접 확인) → 신규 등록 모드에 섹션 미노출 → 기존 필드/탭 회귀 없음. 검증에
쓴 첨부파일(이미지 2개+PDF 1개)은 전부 UI/API 삭제 흐름으로 정리됐다(잔여 0건, storage list API로 재확인).

## 미수정(후속 과제로 명시적으로 미룸)
- `assigned_as_technician`/`processing_technician`의 `employees` 테이블 FK 연동 (1단계는 자유 텍스트).
- 처리점 자동완성/드롭다운 (실제 처리점 목록이 쌓인 뒤 UI에서만).
- 크린어스 vendor 추가 여부 (필요시 후속).
- (발견만, 수정 안 함) `announcements`/`approvals`/`facility-photos` 등 기존 업로드 라우트에 잠재된 동일한
  `supabaseAdmin` 전역 Content-Type 헤더 충돌 — [[dpf]] 스킬 참고. **`announcements/[id]/attachments`는 실제로
  확인됨**: storage list API(읽기 전용)로 조회한 결과 저장된 객체의 `mimetype`이
  `"application/json; charset=utf-8, text/plain"`로 오염되어 있었다(버킷에 `allowedMimeTypes` 제한이 없어 415는
  안 뜨지만 메타데이터가 실제로 깨진 상태). 서명/공개 URL이 이 mimetype을 그대로 Content-Type 헤더로 내보내므로
  브라우저가 다운로드 대신 인라인 렌더링을 시도할 때 영향을 줄 수 있다 — 수정하지 않고 사용자에게 보고.
